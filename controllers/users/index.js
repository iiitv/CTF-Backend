const login = require('./login');
const signup = require('./signup');
const verification = require('./verification');
const resetPassword = require('./resetPassword')

module.exports = { login, signup, verification, resetPassword };

